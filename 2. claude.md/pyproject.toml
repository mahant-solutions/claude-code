[project]
name = "weather-cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
